<UserControl
    x:Class="WhispUI.Settings.Llm.LlmShortcutSlotsSection"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI">

    <StackPanel Spacing="{StaticResource SettingsCardSpacing}">
        <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}"
                   Text="Rewrite shortcuts" />

        <controls:SettingsCard Header="Primary rewrite profile"
                               Description="Profile triggered by Shift + Win + `."
                               HeaderIcon="{ui:FontIcon Glyph=&#xE765;}">
            <ComboBox x:Name="PrimaryRewriteProfileCombo"
                      MinWidth="200"
                      SelectionChanged="PrimaryRewriteProfileCombo_SelectionChanged" />
        </controls:SettingsCard>

        <controls:SettingsCard Header="Secondary rewrite profile"
                               Description="Profile triggered by Ctrl + Win + `. Pick (None) to leave this shortcut unbound."
                               HeaderIcon="{ui:FontIcon Glyph=&#xE765;}">
            <ComboBox x:Name="SecondaryRewriteProfileCombo"
                      MinWidth="200"
                      SelectionChanged="SecondaryRewriteProfileCombo_SelectionChanged" />
        </controls:SettingsCard>
    </StackPanel>
</UserControl>
