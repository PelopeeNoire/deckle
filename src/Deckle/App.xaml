<Application
    x:Class="Deckle.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Ressources partagées par toutes les Settings pages et leurs UserControls.
                 Promues au niveau App pour que les UserControls (Settings/Llm/*) y accèdent
                 sans duplication. WhisperPage/GeneralPage redéfinissent encore localement
                 les mêmes clés (shadow sans effet), sera nettoyé lors de leur refacto. -->
            <x:Double x:Key="SettingsCardSpacing">4</x:Double>
            <Style x:Key="SettingsSectionHeaderTextBlockStyle"
                   BasedOn="{StaticResource BodyStrongTextBlockStyle}"
                   TargetType="TextBlock">
                <Style.Setters>
                    <Setter Property="Margin" Value="1,30,0,6" />
                </Style.Setters>
            </Style>

            <!-- CriticalAccentButtonStyle : destructive action style for
                 Forget / Delete / Remove buttons. WinUI 3 ships
                 SystemFillColorCriticalBrush but NOT Secondary /
                 Tertiary variants of it (those keys exist only for
                 AccentFill, not Critical). We define our own
                 pointer-over / pressed brushes with the Windows 11
                 critical red at progressively darker values, so the
                 destructive signal persists across every interaction
                 state. -->
            <SolidColorBrush x:Key="CriticalButtonBackground"            Color="#C42B1C" />
            <SolidColorBrush x:Key="CriticalButtonBackgroundPointerOver" Color="#B92619" />
            <SolidColorBrush x:Key="CriticalButtonBackgroundPressed"     Color="#A4140A" />
            <Style x:Key="CriticalAccentButtonStyle" TargetType="Button">
                <Setter Property="Foreground"      Value="{ThemeResource TextOnAccentFillColorPrimaryBrush}" />
                <Setter Property="HorizontalAlignment"            Value="Left" />
                <Setter Property="VerticalAlignment"              Value="Center" />
                <Setter Property="HorizontalContentAlignment"     Value="Center" />
                <Setter Property="VerticalContentAlignment"       Value="Center" />
                <Setter Property="Padding"                        Value="{StaticResource ButtonPadding}" />
                <Setter Property="FontFamily"                     Value="{ThemeResource ContentControlThemeFontFamily}" />
                <Setter Property="FontSize"                       Value="{ThemeResource ControlContentThemeFontSize}" />
                <Setter Property="UseSystemFocusVisuals"          Value="{StaticResource UseSystemFocusVisuals}" />
                <Setter Property="FocusVisualMargin"              Value="-3" />
                <Setter Property="MinHeight"                      Value="32" />
                <Setter Property="MinWidth"                       Value="120" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid x:Name="RootGrid"
                                  Background="{StaticResource CriticalButtonBackground}"
                                  BorderBrush="{StaticResource CriticalButtonBackground}"
                                  BorderThickness="{ThemeResource ButtonBorderThemeThickness}"
                                  CornerRadius="{ThemeResource ControlCornerRadius}">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Background"
                                                        Value="{StaticResource CriticalButtonBackgroundPointerOver}" />
                                                <Setter Target="RootGrid.BorderBrush"
                                                        Value="{StaticResource CriticalButtonBackgroundPointerOver}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Background"
                                                        Value="{StaticResource CriticalButtonBackgroundPressed}" />
                                                <Setter Target="RootGrid.BorderBrush"
                                                        Value="{StaticResource CriticalButtonBackgroundPressed}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Background"
                                                        Value="{ThemeResource AccentButtonBackgroundDisabled}" />
                                                <Setter Target="RootGrid.BorderBrush"
                                                        Value="{ThemeResource AccentButtonBorderBrushDisabled}" />
                                                <Setter Target="ContentPresenter.Foreground"
                                                        Value="{ThemeResource AccentButtonForegroundDisabled}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <ContentPresenter x:Name="ContentPresenter"
                                                  BorderBrush="{TemplateBinding BorderBrush}"
                                                  BorderThickness="{TemplateBinding BorderThickness}"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  ContentTransitions="{TemplateBinding ContentTransitions}"
                                                  CornerRadius="{TemplateBinding CornerRadius}"
                                                  Foreground="{TemplateBinding Foreground}"
                                                  HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  Padding="{TemplateBinding Padding}"
                                                  VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  AutomationProperties.AccessibilityView="Raw" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
